defaults:
  - batch_transforms: baseline 
  - _self_

batch_transforms:
  train:
    _target_: src.transforms.NoisyTransform
    flip: False
    erasing: False
    noise: False 
    rotation: True
    road_threshold: 0.25