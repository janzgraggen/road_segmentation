defaults:
  - model: improved 
  - metrics: accuracy
  - datasets: road_test
  - dataloader: main
  - transforms: main
  - _self_
inferencer:
  device_tensors: ["img", "labels"] # which tensors should be on device (ex. GPU)
  device: auto # device name or "auto"
  save_path: "road" # any name here, can be a dataset name
  seed: 1
  from_pretrained: "saved/testing/model_best.pth" # path to the pretrained model
